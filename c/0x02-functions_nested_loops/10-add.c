#include "main.h"

/**
 * add - sum two integer and return the result.
 * @fisrt: first number
 * @last: last number
 * Return: sum of the result
*/

int add(int first, int last)
{
    return (first + last);
}